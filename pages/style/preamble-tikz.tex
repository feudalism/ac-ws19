\usetikzlibrary{shapes,positioning,patterns,
					calc,fit,backgrounds,arrows.meta,
	                 decorations.pathmorphing,
	                 decorations.markings}
	
% % Arrows
\tikzset{
	arrow/.style={draw,-{Latex[length=2.5mm]}},
	vectarrow/.style={draw,double,-{Latex[length=2.5mm]},
        thick, decoration={markings,mark=at position
       1 with {\arrow[semithick]{open triangle 60}}},
       double distance=1.4pt,% shorten >= 5.5pt,
       preaction = {decorate},
       postaction = {draw,line width=1.4pt, white,}%shorten >= 4.5pt}
        },
	vectline/.style={arrow,line width=1.8pt},		
	force/.style={line width=2pt,red,arrow},
		}

\newcommand{\adaptiveBlock}[1]{
    \draw[->,on background layer] 
    ([yshift=-5pt] #1.south west) -- ([yshift=5pt] #1.north east);
}

\newcommand{\bwAbove}[4]{
    ($($(#1)!#3!(#2)$)+(0,#4)$)
}
	
% % Control blocks
\tikzset{
    gain/.style = {%isosceles triangle, isosceles triangle stretches,
        regular polygon, regular polygon sides=3,
        draw, fill=white, text width=0.2cm,
        inner sep=1mm, outer sep=0mm,
        shape border rotate=-90,
        %minimum height=0.1cm,
        minimum width=0.1cm},
    gain flipped/.style = {gain, shape border rotate=90},
    gain up/.style = {gain, shape border rotate=0},
	block/.style={shape=rectangle,draw,semithick,
		inner sep=1mm,minimum width=0.4cm,
		text width=0.4cm,
		align=flush center},
    nonlin/.style={block,double},
	note/.style={
        font=\scriptsize,
		align=flush center
		},
	comp/.style={shape=circle,inner sep=0mm,outer sep=0mm,
		minimum size=2mm,draw,semithick},
	cout/.style={shape=circle,inner sep=0mm,
		minimum size=1.5mm,fill=black,draw},
	ground/.style={fill,pattern=north east lines,minimum width=7pt,
					minimum height=14pt,inner sep=0pt},
	 }


\newcommand{\classDate}[1]{
\tikzset{external/export next=false}
\begin{tikzpicture}[overlay,font=\tiny,align=left,
	xshift={-1.5em},yshift={-1em}]
	\node (date) [date] {};
	\node at (date.center) [text=black!70!white] {\scshape\bfseries #1};
\end{tikzpicture}
}

\makeatletter
\newcommand{\DrawLine}[1]{%
\tikzset{external/export next=false}
  \begin{tikzpicture}
  \path[use as bounding box] (0,0) -- (\linewidth,-0.2);
  \draw[color=black,dashed,dash phase=2pt,#1]
        (0-\kvtcb@leftlower-\kvtcb@boxsep,0)--
        (\linewidth+\kvtcb@rightlower+\kvtcb@boxsep,0);
  \end{tikzpicture}%
  }
\makeatother
